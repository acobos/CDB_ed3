# Solution to exercise 1.4

# Open RStudio and investigate what is your working directory with function getwd(). 
getwd()

# Then change it to another folder of your preference with the menu option:
# Session > Set working directory > Choose Directory.... 

# When done, use again getwd() to see what is now the working directory.
getwd()